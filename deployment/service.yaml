apiVersion: v1
kind: Service
metadata:
  name: loan-api
  namespace: ${KUBERNETES_NAMESPACE}
  labels:
    app: loan-api
spec:
  selector:
    app: loan-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP