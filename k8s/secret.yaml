# Template — do NOT commit real values.
# Create the secret on the cluster with:
#   kubectl apply -f k8s/secret.yaml
#
# Values must be base64-encoded:
#   echo -n "my_value" | base64
#
# SSPCloud credentials are available at:
#   https://datalab.sspcloud.fr → My account → Storage access
apiVersion: v1
kind: Secret
metadata:
  name: loan-api-secret
  namespace: loan-prediction
type: Opaque
data:
  AWS_ACCESS_KEY_ID: <base64_encoded_value>
  AWS_SECRET_ACCESS_KEY: <base64_encoded_value>
  AWS_SESSION_TOKEN: <base64_encoded_value>
  AWS_S3_ENDPOINT: bWluaW8ubGFiLnNzcGNsb3VkLmZy  # minio.lab.sspcloud.fr
  AWS_BUCKET_NAME: <base64_encoded_value>
  MLFLOW_TRACKING_URI: <base64_encoded_mlflow_uri>
